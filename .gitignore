# dependencies
node_modules

# next.js
/.next
/out
*.tsbuildinfo
next-env.d.ts

# env / secrets — never commit (keep .env.example only)
.env
.env.*
!.env.example

# vercel
.vercel

# local tooling
.claude
.models

# generated build artifact (regenerate via: npm run export-sql)
/data/seed.sql

# logs / os
*.log
.DS_Store
